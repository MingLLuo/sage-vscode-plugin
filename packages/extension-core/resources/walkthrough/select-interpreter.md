Select the Sage runtime that should power execution, REPL sessions, runtime-backed docs, and source-root discovery.

[Run Sage: Select Interpreter](command:sage.selectInterpreter)

Prefer a local Sage development checkout for source navigation. Use a system Sage install when you only need editing,
execution, and runtime documentation.
