Open a Sage symbol such as `PolynomialRing`, `matrix`, or a local function, place the cursor on it, then run the UX
self-check.

[Run Sage: Run UX Self Check](command:sage.runUxSelfCheck)

The report should summarize hover, documentation, definition, completion, references, rename preview, signature help,
diagnostics, and any degraded fallback reason.
