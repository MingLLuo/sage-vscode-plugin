{
  "name": "sage-vscode-plugin",
  "private": true,
  "version": "0.1.0",
  "description": "Independent monorepo for a SageMath VS Code plugin",
  "workspaces": [
    "packages/extension-core",
    "packages/syntax-pack"
  ],
  "scripts": {
    "dev:vscode": "./scripts/dev-vscode.sh",
    "dev:vscode:smoke": "./scripts/dev-vscode.sh --smoke",
    "sync:syntax": "node scripts/sync-syntax-assets.mjs",
    "build": "npm run build --workspace @sage-vscode/syntax-pack && npm run build --workspace @sage-vscode/extension-core",
    "lint": "npm run lint --workspace @sage-vscode/syntax-pack && npm run lint --workspace @sage-vscode/extension-core",
    "test": "npm run test --workspace @sage-vscode/syntax-pack && npm run test --workspace @sage-vscode/extension-core && npm run test:python",
    "test:python": "PYTHONPATH=packages/sage-lsp/src python -m pytest packages/sage-lsp/tests"
  }
}
